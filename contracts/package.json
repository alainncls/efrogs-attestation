{
  "name": "efrogs-attestation-contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deployEFrogs.ts --network",
    "lint": "prettier --list-different '**/*.sol' '**/*.ts' '**/*.json' && npm run solhint",
    "lint:fix": "prettier --write '**/*.sol' '**/*.ts' '**/*.json'",
    "solhint": "solhint **/*.sol -c .solhint.json",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.12.7",
    "@verax-attestation-registry/verax-contracts": "^0.0.3",
    "dotenv": "^16.4.5",
    "ethers": "^6.12.0",
    "hardhat": "^2.22.3",
    "prettier": "^3.2.5",
    "prettier-plugin-solidity": "^1.3.1",
    "solhint": "^4.5.4"
  }
}
